# Copy this file to .env and set strong values.
# Never commit the real .env file.

POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
# Optional: leave blank to build the connection string from POSTGRES_*.
DATABASE_URL=
NOTIFICATION_API_KEY=
ENABLE_DOCS=true
